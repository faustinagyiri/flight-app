{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",

    "jsx": "react-jsx",   // ✅ THIS LINE FIXES EVERYTHING

    "strict": true,
    "skipLibCheck": true,

    // Match Vite alias config so imports like "@/components/..." work
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
